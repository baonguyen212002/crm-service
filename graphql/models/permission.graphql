##########################
############# ENUM
##################
enum PermissionsWhereColumn {
    ID @enum(value: "id")
    NAME @enum(value: "name")
}
enum PermissionsOrderByColumn {
    createdAt @enum(value: "created_at")
}

##########################
############# TYPE
##################
type Permission {
    id: ID
    name: String
}
